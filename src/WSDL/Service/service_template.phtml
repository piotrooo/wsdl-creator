<!DOCTYPE html>
<html>
<head>
    <title>ExampleClass :: WSDL Creator</title>
    <style>
        @import url(http://fonts.googleapis.com/css?family=Dosis:400,200,300,500,600);

        body {
            margin: 0;
            padding: 0;
            font-family: Dosis, Arial, sans-serif;
        }

        div#header {
            padding: 15px 15px 15px 70px;
            background-color: #003366;
            color: #FFFFFF;
            font-size: 25px;
        }

        div#content {
            margin: 0 auto;
            width: 90%;
        }

        div#footer {
            border-top: 1px #FAE8CD solid;
            background-color: #003366;
            color: #FFFFFF;
            margin-top: 20px;
            padding: 15px 70px 15px 15px;
            text-align: right;
            font-size: 10px;
        }

        div#footer a {
            color: #FFFFFF;
            text-decoration: none;
        }

        div#footer a:hover {
            text-decoration: underline;
        }

        div.method p.name {
            border-bottom: 1px #128085 dotted;
            font-size: 19px;
        }

        div.method-content {
            margin-left: 30px;
        }

        div.border {
            border: 1px #128085 dotted;
            padding-left: 10px;
            background-color: #E5E5CC;
            font-family: Courier New, monospace !important;
            font-size: 13px;
        }

        div.parameters ul {
            padding: 0;
            list-style: none;
        }

        pre {
            font-family: Courier New, monospace !important;
        }
    </style>
</head>
<body>
<div id="root">
    <div id="header"><?= $this->template->serviceName ?> Service (<?= $this->template->serviceUrl ?>)</div>
    <div id="content">
        <? foreach ($this->template->methods as $method): ?>
            <div class="method">
                <p class="name"><?= $method->getName() ?></p>

                <div class="method-content">
                    <div class="parameters">
                        <p>Parameters:</p>

                        <div class="border">
                            <ul>
                                <? foreach ($method->getRawParameters() as $parameter): ?>
                                    <li><?= $parameter ?></li>
                                <? endforeach; ?>
                            </ul>
                        </div>
                    </div>
                    <div class="return">
                        <p>Return:</p>

                        <div class="border">
                            <p><?= $method->getRawReturn() ?></p>
                        </div>
                    </div>
                    <div class="request">
                        <p>Request:</p>

                        <div class="border">
                            <pre><?= htmlspecialchars($method->getMethodSampleRequest($this->template->serviceUrl)->getXML()) ?></pre>
                        </div>
                    </div>
                </div>
            </div>
        <? endforeach; ?>
    </div>
    <div id="footer">
        Generated by <a target="_blank" href="https://github.com/piotrooo/wsdl-creator">wsdl-creator</a>
    </div>
</div>
</body>
</html>